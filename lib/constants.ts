/**
 * Shared application-wide constants. Keep cross-cutting values here so a number
 * like the max ranked roles is defined in exactly one place.
 */

/** Maximum number of roles an applicant may rank (per campaign). */
export const MAX_ROLE_RANKING = 5;
